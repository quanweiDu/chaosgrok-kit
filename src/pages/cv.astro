---
import Layout from '../layouts/Layout.astro';
---

<Layout title="CV — MR.QuanweiDU" description="Curriculum Vitae">

<div class="cv-wrap">

  <!-- 左侧固定栏 -->
  <aside class="cv-sidebar">
    <div class="sidebar-inner">
      <div class="cv-name">
        <span class="name-en">Quanwei</span>
        <span class="name-divider">/</span>
        <span class="name-zh">杜全威</span>
      </div>
      <div class="cv-tagline">Designer · Thinker · Builder</div>

      <nav class="cv-nav">
        <a href="#profile" class="cv-nav-item">Profile</a>
        <a href="#experience" class="cv-nav-item">Experience</a>
        <a href="#skills" class="cv-nav-item">Skills</a>
        <a href="#projects" class="cv-nav-item">Projects</a>
        <a href="#contact" class="cv-nav-item">Contact</a>
      </nav>

      <div class="sidebar-footer">
        <a href="/" class="back-link">← chaosgrok.com</a>
        <div class="cv-availability">
          <span class="avail-dot"></span>
          Available for work
        </div>
      </div>
    </div>
  </aside>

  <!-- 右侧主内容 -->
  <main class="cv-main">

    <!-- HERO -->
    <section class="cv-hero" id="profile">
      <div class="hero-index">001</div>
      <h1 class="hero-title">
        I build things<br>
        that <em>matter.</em>
      </h1>
      <p class="hero-bio">
        在混沌中寻找结构，在复杂中提炼清晰。
        多年来专注于设计、思维与创造的交叉地带，
        相信真正好的工作来自对本质的理解，而非对表面的模仿。
      </p>
      <div class="hero-meta">
        <div class="meta-item">
          <span class="meta-label">Location</span>
          <span class="meta-value">China · Remote</span>
        </div>
        <div class="meta-item">
          <span class="meta-label">Available</span>
          <span class="meta-value">Full-time · Freelance</span>
        </div>
        <div class="meta-item">
          <span class="meta-label">Languages</span>
          <span class="meta-value">中文 · English</span>
        </div>
      </div>
    </section>

    <div class="cv-divider"></div>

    <!-- EXPERIENCE -->
    <section class="cv-section" id="experience">
      <div class="section-header">
        <span class="section-index">002</span>
        <h2 class="section-title">Experience</h2>
      </div>

      <div class="timeline">

        <div class="timeline-item reveal">
          <div class="timeline-date">
            <span>2022</span>
            <span class="date-sep">—</span>
            <span>Present</span>
          </div>
          <div class="timeline-content">
            <div class="job-title">Independent Consultant</div>
            <div class="job-company">Self-employed · chaosgrok.com</div>
            <p class="job-desc">
              一对一深度咨询，帮助创业者与个人在关键决策节点找到清晰路径。
              专注思维框架建构、问题拆解与行动方案设计。
            </p>
            <div class="job-tags">
              <span class="tag">Strategic Thinking</span>
              <span class="tag">1-on-1 Consulting</span>
              <span class="tag">Problem Framing</span>
            </div>
          </div>
        </div>

        <div class="timeline-item reveal">
          <div class="timeline-date">
            <span>2019</span>
            <span class="date-sep">—</span>
            <span>2022</span>
          </div>
          <div class="timeline-content">
            <div class="job-title">Senior Designer</div>
            <div class="job-company">Company Name · City</div>
            <p class="job-desc">
              主导多个核心产品的视觉与交互设计，从0到1搭建设计系统。
              与产品、研发团队深度协作，推动设计驱动的决策文化。
            </p>
            <div class="job-tags">
              <span class="tag">Product Design</span>
              <span class="tag">Design System</span>
              <span class="tag">Team Leadership</span>
            </div>
          </div>
        </div>

        <div class="timeline-item reveal">
          <div class="timeline-date">
            <span>2016</span>
            <span class="date-sep">—</span>
            <span>2019</span>
          </div>
          <div class="timeline-content">
            <div class="job-title">Designer / Creative</div>
            <div class="job-company">Company Name · City</div>
            <p class="job-desc">
              负责品牌视觉、内容创作与用户体验设计。
              建立了对美学与功能平衡的深层理解。
            </p>
            <div class="job-tags">
              <span class="tag">Brand Design</span>
              <span class="tag">Visual Identity</span>
              <span class="tag">Content</span>
            </div>
          </div>
        </div>

      </div>
    </section>

    <div class="cv-divider"></div>

    <!-- SKILLS -->
    <section class="cv-section" id="skills">
      <div class="section-header">
        <span class="section-index">003</span>
        <h2 class="section-title">Skills</h2>
      </div>

      <div class="skills-grid">
        <div class="skill-group reveal">
          <div class="skill-group-title">Design</div>
          <div class="skill-list">
            <div class="skill-item">
              <span class="skill-name">Visual Design</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 95%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">UI / UX</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 90%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Typography</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 88%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Brand Identity</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 85%"></div></div>
            </div>
          </div>
        </div>

        <div class="skill-group reveal">
          <div class="skill-group-title">Build</div>
          <div class="skill-list">
            <div class="skill-item">
              <span class="skill-name">HTML / CSS</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 80%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Astro / Web</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 75%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Figma</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 92%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Motion Design</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 70%"></div></div>
            </div>
          </div>
        </div>

        <div class="skill-group reveal">
          <div class="skill-group-title">Think</div>
          <div class="skill-list">
            <div class="skill-item">
              <span class="skill-name">Strategic Consulting</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 90%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Problem Framing</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 93%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Writing</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 85%"></div></div>
            </div>
            <div class="skill-item">
              <span class="skill-name">Research</span>
              <div class="skill-bar"><div class="skill-fill" style="width: 80%"></div></div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <div class="cv-divider"></div>

    <!-- PROJECTS -->
    <section class="cv-section" id="projects">
      <div class="section-header">
        <span class="section-index">004</span>
        <h2 class="section-title">Projects</h2>
      </div>

      <div class="projects-grid">
        <a href="/" class="project-card reveal">
          <div class="project-num">P.01</div>
          <div class="project-title">chaosgrok.com</div>
          <div class="project-desc">个人网站，集写作、音乐、影像与咨询于一体。独立设计开发，完全自主部署。</div>
          <div class="project-link">查看 ↗</div>
        </a>
        <div class="project-card reveal">
          <div class="project-num">P.02</div>
          <div class="project-title">Project Title</div>
          <div class="project-desc">项目简介，一两句话说清楚做了什么、解决了什么问题。</div>
          <div class="project-link">查看 ↗</div>
        </div>
        <div class="project-card reveal">
          <div class="project-num">P.03</div>
          <div class="project-title">Project Title</div>
          <div class="project-desc">项目简介，一两句话说清楚做了什么、解决了什么问题。</div>
          <div class="project-link">查看 ↗</div>
        </div>
      </div>
    </section>

    <div class="cv-divider"></div>

    <!-- CONTACT -->
    <section class="cv-section cv-contact" id="contact">
      <div class="section-header">
        <span class="section-index">005</span>
        <h2 class="section-title">Contact</h2>
      </div>

      <div class="contact-block reveal">
        <p class="contact-headline">如果你在找一个<br>能<em>真正思考</em>的人。</p>
        <div class="contact-links">
          <a href="mailto:3690413@gmail.com" class="contact-item">
            <span class="contact-label">Email</span>
            <span class="contact-value">3690413@gmail.com</span>
          </a>
          <a href="/" class="contact-item">
            <span class="contact-label">Website</span>
            <span class="contact-value">chaosgrok.com</span>
          </a>
          <div class="contact-item">
            <span class="contact-label">WeChat</span>
            <span class="contact-value">your_wechat_id</span>
          </div>
        </div>
      </div>
    </section>

  </main>

</div>

</Layout>

<script>
  // 平滑滚动
  document.querySelectorAll('.cv-nav-item').forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const id = link.getAttribute('href')?.slice(1);
      document.getElementById(id!)?.scrollIntoView({ behavior: 'smooth' });
    });
  });

  // 激活导航高亮
  const sections = document.querySelectorAll('section[id]');
  const navItems = document.querySelectorAll('.cv-nav-item');

  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        navItems.forEach(item => item.classList.remove('active'));
        const active = document.querySelector(`.cv-nav-item[href="#${entry.target.id}"]`);
        active?.classList.add('active');
      }
    });
  }, { threshold: 0.3 });

  sections.forEach(s => observer.observe(s));

  // Reveal动效
  const revealObs = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('visible');
        revealObs.unobserve(entry.target);
      }
    });
  }, { threshold: 0.1 });

  document.querySelectorAll('.reveal').forEach(el => revealObs.observe(el));

  // 技能条动画
  document.querySelectorAll('.skill-fill').forEach(bar => {
    const width = (bar as HTMLElement).style.width;
    (bar as HTMLElement).style.width = '0';
    setTimeout(() => {
      (bar as HTMLElement).style.width = width;
    }, 300);
  });
</script>

<style>
  .cv-wrap {
    display: grid;
    grid-template-columns: 260px 1fr;
    min-height: 100vh;
    max-width: 1280px;
    margin: 0 auto;
  }

  /* ── SIDEBAR ── */
  .cv-sidebar {
    position: sticky;
    top: 0;
    height: 100vh;
    border-right: 1px solid var(--rule);
    padding: 60px 40px;
    display: flex;
    flex-direction: column;
  }

  .sidebar-inner {
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .cv-name {
    margin-bottom: 8px;
  }

  .name-en {
    font-family: var(--serif-en);
    font-size: 18px;
    font-weight: 400;
    color: var(--ink);
    letter-spacing: -0.01em;
  }

  .name-divider {
    font-family: var(--serif-en);
    color: var(--rule);
    margin: 0 6px;
    font-size: 14px;
  }

  .name-zh {
    font-family: var(--serif-zh);
    font-size: 14px;
    color: var(--ink-light);
  }

  .cv-tagline {
    font-family: var(--serif-en);
    font-size: 10px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ink-ghost);
    margin-bottom: 56px;
  }

  .cv-nav {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .cv-nav-item {
    font-family: var(--serif-en);
    font-size: 12px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--ink-ghost);
    text-decoration: none;
    padding: 10px 0;
    border-bottom: 1px solid transparent;
    transition: all 0.2s;
    position: relative;
  }

  .cv-nav-item::before {
    content: '';
    position: absolute;
    left: -16px;
    top: 50%;
    transform: translateY(-50%);
    width: 4px;
    height: 4px;
    background: var(--ink);
    border-radius: 50%;
    opacity: 0;
    transition: opacity 0.2s;
  }

  .cv-nav-item:hover,
  .cv-nav-item.active {
    color: var(--ink);
  }

  .cv-nav-item.active::before {
    opacity: 1;
  }

  .sidebar-footer {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .back-link {
    font-family: var(--serif-en);
    font-size: 11px;
    letter-spacing: 0.1em;
    color: var(--ink-ghost);
    text-decoration: none;
    transition: color 0.2s;
  }

  .back-link:hover { color: var(--ink); }

  .cv-availability {
    display: flex;
    align-items: center;
    gap: 8px;
    font-family: var(--serif-en);
    font-size: 11px;
    color: var(--ink-ghost);
    letter-spacing: 0.05em;
  }

  .avail-dot {
    width: 6px;
    height: 6px;
    background: #4caf50;
    border-radius: 50%;
    animation: pulse 2s infinite;
  }

  @keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.4; }
  }

  /* ── MAIN ── */
  .cv-main {
    padding: 0 80px;
  }

  .cv-divider {
    height: 1px;
    background: var(--rule);
    margin: 0;
  }

  /* ── HERO ── */
  .cv-hero {
    padding: 100px 0 80px;
    position: relative;
  }

  .hero-index {
    font-family: var(--serif-en);
    font-size: 10px;
    letter-spacing: 0.3em;
    color: var(--ink-ghost);
    margin-bottom: 40px;
    opacity: 0;
    animation: fadeUp 0.6s ease 0.1s forwards;
  }

  .hero-title {
    font-family: var(--serif-en);
    font-size: clamp(48px, 6vw, 80px);
    font-weight: 400;
    line-height: 1.1;
    letter-spacing: -0.02em;
    color: var(--ink);
    margin: 0 0 40px;
    opacity: 0;
    animation: fadeUp 0.6s ease 0.2s forwards;
  }

  .hero-title em {
    font-style: italic;
    color: var(--ink-light);
  }

  .hero-bio {
    font-family: var(--serif-zh);
    font-size: 15px;
    color: var(--ink-light);
    line-height: 2.2;
    max-width: 560px;
    margin: 0 0 56px;
    opacity: 0;
    animation: fadeUp 0.6s ease 0.3s forwards;
  }

  .hero-meta {
    display: flex;
    gap: 48px;
    opacity: 0;
    animation: fadeUp 0.6s ease 0.4s forwards;
  }

  .meta-item {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .meta-label {
    font-family: var(--serif-en);
    font-size: 10px;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--ink-ghost);
  }

  .meta-value {
    font-family: var(--serif-en);
    font-size: 13px;
    color: var(--ink);
    letter-spacing: 0.02em;
  }

  /* ── SECTIONS ── */
  .cv-section {
    padding: 80px 0;
  }

  .section-header {
    display: flex;
    align-items: baseline;
    gap: 20px;
    margin-bottom: 56px;
  }

  .section-index {
    font-family: var(--serif-en);
    font-size: 10px;
    letter-spacing: 0.3em;
    color: var(--ink-ghost);
    font-style: italic;
  }

  .section-title {
    font-family: var(--serif-en);
    font-size: 13px;
    font-weight: 400;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: var(--ink);
    margin: 0;
  }

  /* ── TIMELINE ── */
  .timeline {
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .timeline-item {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 40px;
    padding: 40px 0;
    border-top: 1px solid var(--rule);
  }

  .timeline-item:last-child {
    border-bottom: 1px solid var(--rule);
  }

  .timeline-date {
    display: flex;
    flex-direction: column;
    gap: 2px;
    font-family: var(--serif-en);
    font-size: 12px;
    color: var(--ink-ghost);
    letter-spacing: 0.05em;
    padding-top: 4px;
  }

  .date-sep { color: var(--rule); }

  .job-title {
    font-family: var(--serif-en);
    font-size: 18px;
    font-weight: 400;
    color: var(--ink);
    margin-bottom: 6px;
    letter-spacing: -0.01em;
  }

  .job-company {
    font-family: var(--serif-en);
    font-size: 12px;
    color: var(--ink-ghost);
    letter-spacing: 0.05em;
    margin-bottom: 20px;
  }

  .job-desc {
    font-family: var(--serif-zh);
    font-size: 14px;
    color: var(--ink-light);
    line-height: 2.1;
    margin: 0 0 20px;
  }

  .job-tags {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
  }

  .tag {
    font-family: var(--serif-en);
    font-size: 10px;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: var(--ink-ghost);
    border: 1px solid var(--rule);
    padding: 4px 10px;
  }

  /* ── SKILLS ── */
  .skills-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 48px;
  }

  .skill-group-title {
    font-family: var(--serif-en);
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ink-ghost);
    margin-bottom: 28px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--rule);
  }

  .skill-list { display: flex; flex-direction: column; gap: 16px; }

  .skill-item {
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .skill-name {
    font-family: var(--serif-en);
    font-size: 12px;
    color: var(--ink-light);
    letter-spacing: 0.03em;
  }

  .skill-bar {
    height: 1px;
    background: var(--rule);
    width: 100%;
  }

  .skill-fill {
    height: 100%;
    background: var(--ink);
    transition: width 1.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  /* ── PROJECTS ── */
  .projects-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2px;
  }

  .project-card {
    padding: 36px 32px;
    border: 1px solid var(--rule);
    text-decoration: none;
    color: inherit;
    transition: border-color 0.3s;
    display: block;
    cursor: default;
  }

  a.project-card { cursor: pointer; }
  a.project-card:hover { border-color: var(--ink-ghost); }

  .project-num {
    font-family: var(--serif-en);
    font-size: 10px;
    font-style: italic;
    color: var(--ink-ghost);
    margin-bottom: 20px;
    letter-spacing: 0.1em;
  }

  .project-title {
    font-family: var(--serif-en);
    font-size: 16px;
    font-weight: 400;
    color: var(--ink);
    margin-bottom: 12px;
  }

  .project-desc {
    font-family: var(--serif-zh);
    font-size: 13px;
    color: var(--ink-light);
    line-height: 2;
    margin-bottom: 24px;
  }

  .project-link {
    font-family: var(--serif-en);
    font-size: 11px;
    letter-spacing: 0.1em;
    color: var(--ink-ghost);
    transition: color 0.2s;
  }

  a.project-card:hover .project-link { color: var(--ink); }

  /* ── CONTACT ── */
  .cv-contact { padding-bottom: 120px; }

  .contact-headline {
    font-family: var(--serif-en);
    font-size: clamp(32px, 4vw, 52px);
    font-weight: 400;
    line-height: 1.2;
    letter-spacing: -0.01em;
    color: var(--ink);
    margin: 0 0 56px;
  }

  .contact-headline em {
    font-style: italic;
    color: var(--ink-light);
  }

  .contact-links {
    display: flex;
    flex-direction: column;
    gap: 0;
    max-width: 480px;
  }

  .contact-item {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 0;
    border-top: 1px solid var(--rule);
    text-decoration: none;
    color: inherit;
    transition: opacity 0.2s;
  }

  .contact-item:last-child { border-bottom: 1px solid var(--rule); }
  a.contact-item:hover { opacity: 0.6; }

  .contact-label {
    font-family: var(--serif-en);
    font-size: 11px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--ink-ghost);
  }

  .contact-value {
    font-family: var(--serif-en);
    font-size: 13px;
    color: var(--ink);
    letter-spacing: 0.02em;
  }

  /* ── ANIMATIONS ── */
  @keyframes fadeUp {
    from { opacity: 0; transform: translateY(20px); }
    to   { opacity: 1; transform: translateY(0); }
  }

  .reveal {
    opacity: 0;
    transform: translateY(24px);
    transition: opacity 0.7s ease, transform 0.7s ease;
  }

  .reveal.visible {
    opacity: 1;
    transform: translateY(0);
  }

  /* ── MOBILE ── */
  @media (max-width: 900px) {
    .cv-wrap {
      grid-template-columns: 1fr;
    }

    .cv-sidebar {
      position: relative;
      height: auto;
      padding: 48px 24px 32px;
      border-right: none;
      border-bottom: 1px solid var(--rule);
    }

    .cv-nav {
      flex-direction: row;
      flex-wrap: wrap;
      gap: 0;
    }

    .cv-nav-item { padding: 8px 16px 8px 0; }
    .cv-nav-item::before { display: none; }

    .sidebar-footer {
      flex-direction: row;
      align-items: center;
      justify-content: space-between;
      margin-top: 24px;
    }

    .cv-main { padding: 0 24px; }

    .timeline-item {
      grid-template-columns: 1fr;
      gap: 16px;
    }

    .timeline-date { flex-direction: row; gap: 6px; }

    .skills-grid { grid-template-columns: 1fr; gap: 40px; }
    .projects-grid { grid-template-columns: 1fr; }

    .hero-meta { flex-wrap: wrap; gap: 28px; }
  }
</style>

</Layout>
