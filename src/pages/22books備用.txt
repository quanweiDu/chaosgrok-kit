---
// src/pages/books.astro
// 书架页面 + Lemon Squeezy 购买按钮
// 使用方法：把每本书的 lemonsqueezyUrl 换成你真实的购买链接

import { getCollection } from 'astro:content';

const books = await getCollection('books');

// 手动配置每本书的 Lemon Squeezy 购买链接
// 格式：{ [书的slug]: "https://你的店铺.lemonsqueezy.com/checkout/buy/xxx" }
const buyLinks: Record<string, string> = {
  // 'book-slug': 'https://yourstore.lemonsqueezy.com/checkout/buy/xxx',
  // 注册 Lemon Squeezy 后把链接填进来
};
---

<html lang="zh">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1"/>
  <title>Books · chaosgrok</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      background: #0d0d0d;
      color: #e5e5e5;
      font-family: 'Georgia', serif;
      min-height: 100vh;
      padding: 80px 24px;
    }

    .page-header {
      max-width: 800px;
      margin: 0 auto 64px;
    }

    .page-title {
      font-size: 13px;
      letter-spacing: 0.3em;
      text-transform: uppercase;
      color: #444;
      margin-bottom: 12px;
    }

    .page-desc {
      font-size: 15px;
      color: #555;
      line-height: 1.7;
    }

    .books-grid {
      max-width: 800px;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      gap: 48px;
    }

    .book-card {
      display: grid;
      grid-template-columns: 140px 1fr;
      gap: 32px;
      padding-bottom: 48px;
      border-bottom: 1px solid #1a1a1a;
    }

    .book-card:last-child {
      border-bottom: none;
    }

    .book-cover {
      width: 140px;
      height: 196px;
      background: #1a1a1a;
      border: 1px solid #222;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
      overflow: hidden;
    }

    .book-cover img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .book-cover-placeholder {
      font-size: 10px;
      letter-spacing: 0.15em;
      color: #333;
      text-transform: uppercase;
      text-align: center;
      padding: 16px;
    }

    .book-info {
      display: flex;
      flex-direction: column;
      justify-content: space-between;
      gap: 20px;
    }

    .book-meta { display: flex; flex-direction: column; gap: 12px; }

    .book-title {
      font-size: 20px;
      font-weight: normal;
      color: #e5e5e5;
      line-height: 1.4;
    }

    .book-title-zh {
      font-size: 13px;
      color: #444;
      letter-spacing: 0.05em;
    }

    .book-desc {
      font-size: 14px;
      color: #666;
      line-height: 1.8;
    }

    .book-tags {
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
    }

    .tag {
      font-size: 10px;
      letter-spacing: 0.15em;
      text-transform: uppercase;
      color: #333;
      border: 1px solid #222;
      padding: 3px 8px;
    }

    .book-footer {
      display: flex;
      align-items: center;
      gap: 20px;
      flex-wrap: wrap;
    }

    .book-price {
      font-size: 22px;
      color: #e5e5e5;
      letter-spacing: -0.02em;
    }

    .book-price .currency {
      font-size: 13px;
      color: #555;
      margin-right: 2px;
    }

    /* 购买按钮 */
    .btn-buy {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      background: #e5e5e5;
      color: #0d0d0d;
      font-size: 11px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      padding: 12px 24px;
      text-decoration: none;
      border: none;
      cursor: pointer;
      transition: background 0.2s, color 0.2s;
    }

    .btn-buy:hover {
      background: #fff;
    }

    /* 即将上架 */
    .btn-soon {
      display: inline-flex;
      align-items: center;
      font-size: 11px;
      letter-spacing: 0.2em;
      text-transform: uppercase;
      color: #333;
      border: 1px solid #222;
      padding: 12px 24px;
      cursor: not-allowed;
    }

    .format-badge {
      font-size: 10px;
      letter-spacing: 0.1em;
      text-transform: uppercase;
      color: #444;
    }

    @media (max-width: 600px) {
      .book-card {
        grid-template-columns: 1fr;
      }
      .book-cover {
        width: 100%;
        height: 200px;
      }
    }
  </style>
</head>
<body>

  <div class="page-header">
    <div class="page-title">Books</div>
    <div class="page-desc">混沌里写下来的东西。</div>
  </div>

  <div class="books-grid">
    {books.length === 0 ? (
      <!-- 没有内容集合数据时的占位 -->
      <div style="color:#333; font-size:13px; letter-spacing:0.1em;">暂无书籍，稍后上架。</div>
    ) : (
      books.map((book) => {
        const url = buyLinks[book.slug];
        return (
          <div class="book-card">

            <div class="book-cover">
              {book.data.cover
                ? <img src={book.data.cover} alt={book.data.title}/>
                : <div class="book-cover-placeholder">chaosgrok</div>
              }
            </div>

            <div class="book-info">
              <div class="book-meta">
                <div>
                  <h2 class="book-title">{book.data.title}</h2>
                  {book.data.titleZh && (
                    <div class="book-title-zh">{book.data.titleZh}</div>
                  )}
                </div>

                {book.data.description && (
                  <p class="book-desc">{book.data.description}</p>
                )}

                {book.data.tags && book.data.tags.length > 0 && (
                  <div class="book-tags">
                    {book.data.tags.map((tag: string) => (
                      <span class="tag">{tag}</span>
                    ))}
                  </div>
                )}
              </div>

              <div class="book-footer">
                {book.data.price && (
                  <div class="book-price">
                    <span class="currency">¥</span>{book.data.price}
                  </div>
                )}

                {url ? (
                  <a href={url} target="_blank" class="btn-buy">
                    立即购买 →
                  </a>
                ) : (
                  <span class="btn-soon">即将上架</span>
                )}

                <span class="format-badge">PDF · 数字版</span>
              </div>
            </div>

          </div>
        );
      })
    )}
  </div>

</body>
</html>
